function n() {
  let x = window.prompt("enter a number");
  let y = window.prompt("enter a number");
  // window.alert(x / y);
  // window.alert(Number(x) + Number(y));
  // window.alert(x * y);
  // window.alert(x - y);
  window.alert((x / y) * 100);
}
n();
